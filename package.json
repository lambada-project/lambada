{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^9.0.3"
  },
  "scripts": {
    "clean": "lerna clean",
    "build": "lerna run build",
		"test": "lerna run test --no-bail --no-sort",
    "update": "npm run lb && lerna run update-packages",
    "lerna:publish": "npm run build && lerna publish from-package"
  },
  "workspaces": [
    "packages/core",
    "packages/create",
    "packages/utils"
  ],
  "overrides": {
    "fast-xml-parser": "5.7.1"
  }
}
