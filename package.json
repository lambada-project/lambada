{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^9.0.1"
  },
  "scripts": {
    "clean": "lerna clean",
    "build": "lerna run build",
    "lerna:publish": "npm run build && lerna publish from-package"
  },
  "workspaces": [
    "packages/core",
    "packages/create",
    "packages/utils"
  ]
}
